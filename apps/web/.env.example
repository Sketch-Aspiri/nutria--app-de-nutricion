# Clave del proveedor de IA (solo se usa en el servidor, nunca se expone al cliente).
# Sin esta variable, las funciones de IA muestran un error amigable y el resto de la app funciona.
ANTHROPIC_API_KEY=
