import { MensajesCliente } from '@/features/mensajes/MensajesCliente';

export const metadata = { title: 'Mensajes — nutria' };

export default function MensajesPage() {
  return <MensajesCliente />;
}
